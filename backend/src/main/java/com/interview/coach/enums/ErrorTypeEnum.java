package com.interview.coach.enums;

public enum ErrorTypeEnum {
    SYNTAX_ERROR,
    LOGIC_ERROR,
    BOUNDARY_ERROR,
    ALGORITHM_ERROR,
    TIMEOUT,
    RUNTIME_ERROR,
    SYSTEM_ERROR,
    ACCEPTED_REVIEW
}
